# Requirements for {{PROCESS_NAME}} (Python DDS Process)
# GENERATED BY /rti_dev — DDS Process Builder

# RTI Connext DDS Python API (installed from NDDSHOME)
# rti.connextdds  — installed via: pip install $NDDSHOME/resource/python_api/<arch>/rti-*.whl

# Test dependencies
pytest>=7.0
pytest-asyncio>=0.21
