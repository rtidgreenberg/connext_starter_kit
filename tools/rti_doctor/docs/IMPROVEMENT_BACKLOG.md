# RTI Doctor Improvement Backlog

## Scope

This backlog follows the P0 Connext/Cyclone and Connext/Fast DDS fault-injection
work and the Fast DDS RTPS capture in `test_output/fastdds_rtps/`. It is the
canonical list of deferred and post-current-implementation improvements. It
separates test-harness reliability from RTI Doctor product behavior so a
passing test always means the intended thing.

## Priority 0: Unlock Reliable Fast DDS P0 Coverage

| ID | Improvement | Evidence | Implementation | Acceptance check |
|---|---|---|---|---|
| FDD-1 | Implemented: deterministic Doctor/Fast DDS endpoint readiness. | Fast DDS emits SEDP endpoint DATA immediately; a late Doctor observer reports `qos.no_counterpart`. | Doctor writes `--ready-file` after participant creation. The Fast DDS fixture waits on a start file and writes an endpoint-created marker; the test releases the writer only after a Fast DDS reader confirms endpoint creation. | Passed: ten Connext BEST_EFFORT writer -> Fast DDS RELIABLE reader runs each discovered the reader and reported active `qos.rxo_mismatch`. |
| FDD-2 | Partially implemented: resolve Fast DDS custom TypeObject compatibility variants. | The default custom FINAL TypeObject, default metadata, and TypeLookup controls now resolve `DynamicType`; deliberate metadata suppression remains unresolved by design. Connext's `--type-object-v1-only` experiment does not discover this Fast DDS writer and remains an expected failure. | The custom TypeObject test uses the deterministic endpoint-ready protocol and now requires default-mode resolution. Keep the v1-only experiment isolated; capture `PID_TYPE_INFORMATION` only if current default support regresses. Upgrade the fixture before investigating any reported vendor issue; do not maintain older-version comparison lanes. | Passed for supported default mode: the custom Fast DDS writer resolves `DynamicType`. The v1-only discovery experiment remains a separately tracked expected failure. |
| FDD-3 | Implemented: promote Fast DDS P0 tests where Docker is provisioned. | The class now has deterministic endpoint ordering; the custom Fast DDS FINAL TypeObject variant remains an isolated expected `type.assignability` outcome. | Class and reverse-direction skips removed. Retain explicit skips for absent Docker or image. | Passing: both directions run; reliability faults report exactly one active `qos.rxo_mismatch` naming RELIABILITY. The reverse healthy case separately asserts resolved metadata and the known custom FINAL TypeObject finding. |

## Priority 1: Improve Test Determinism And Evidence

| ID | Improvement | Evidence | Implementation | Acceptance check |
|---|---|---|---|---|
| HAR-1 | Implemented: replace sleep-based fixture ordering with discovery-aware readiness. | Participant-created markers alone did not prove Doctor had observed remote peers; Fast DDS one-shot SEDP announcements could still be missed. | Both endpoints create participants behind file gates. Doctor waits for the requested remote-participant count before writing its marker, then the test releases and confirms reader and writer endpoint creation in order. | Passed: all four Fast DDS P0 controls and ten consecutive Connext BEST_EFFORT writer -> Fast DDS RELIABLE reader fault runs discovered the endpoints and emitted active `qos.rxo_mismatch`. |
| HAR-2 | Implemented: deterministic safe DDS domains. | Eight suites each picked from their own private `random.randint` band; one reached domain 230, whose RTPS ports sit just under the 16-bit ceiling. A collision was indistinguishable from the fault under test and was not reproducible from the failure output. | `test/domains.py` derives a port-safe domain from a stable key and refuses any whose range is privileged or wrapped. All eight suites use it. `RTI_DOCTOR_DOMAIN_OFFSET` shifts a machine's whole band so determinism does not make two developers collide every run. | Passed: `test_domains.py` asserts the port mapping, the ceiling, that no two of the repo's suites collide, and that a malformed offset is ignored rather than crashing every suite. |
| HAR-3 | Implemented: retain failure artifacts automatically. | RTPS capture was necessary to distinguish endpoint visibility from discovery timing. | On failure, write process stdout/stderr and command metadata below `test_output/rti_doctor_faults/<run-id>/`, with a copy of the readiness-control directory; honor `RTI_DOCTOR_KEEP_ARTIFACTS=1` for successful debugging runs. | A retained successful Fast DDS P0 bundle contained commands, all three process streams, Doctor log, and the Doctor/reader/writer start and ready markers without overwriting another run. |
| HAR-4 | Implemented: centralize headless Doctor execution and report parsing. | The P0 suite had duplicate CLI commands and Fast DDS can write a non-JSON Connext discovery diagnostic before JSON. | `test/doctor_e2e.py` builds the isolated CLI environment and extracts the report JSON after native preambles. Fault and wire E2E suites use it; the wire suite retains its topic-not-found retry before parsing. Parse failures include command, stdout, and stderr. | Focused helper tests cover clean reports, native preambles, malformed preambles, and non-report JSON. Fault and wire suites share the same report parser. |
| HAR-6 | **No longer reproduces as described (2026-08-11); cause not established.** `./run_tests.sh vendor` on this host is now green: 28 tests, **0 failures**, 12 skipped, 1 expected failure. All three tests this entry names pass - both `test_fastdds_extensibility_vendor_e2e` matrices and `test_fastdds_type_object_e2e`. **Green a second time on 2026-08-11 at `11b6776`** (32 tests, 12 skipped, 2 expected failures - the tier grew by the Fast DDS representation spike since the run above), again with no product change to explain it. Two consecutive passes is a stronger green than one and still not a cause. Nothing in the product changed that would explain it, so the likeliest cause is the image rebuild this entry already flagged as "the first thing to rule out". **Do not close it on that basis**: an intermittent cross-vendor match failure that stops reproducing is the worst kind of green. What is still owed is the triage below - establish *why* it passes now, against a pinned image tag - before the vendor tier is trusted as a gate. Original entry follows. Triage three Fast DDS vendor e2e failures. | `./run_tests.sh vendor` is red: 28 tests, **3 failures**, 12 skipped, 1 expected failure. Two are `test_fastdds_extensibility_vendor_e2e` (`final` and `appendable`) where the Fast DDS writer reports `matched: 0` after writing 100+ samples, i.e. Fast DDS never matched the Connext reader; the third is `test_fastdds_type_object_e2e`, where the Fast DDS writer never creates its endpoint marker. **Not caused by the HAR-2 migration**: the only change to those files was the domain source, and both failures reproduce identically at domain 167 and at domain 30. Note the `rti-doctor-fastdds-e2e` images were rebuilt shortly before this run, which is the first thing to rule out. | Establish whether this is an image/version regression, an environment issue, or a genuine Connext/Fast DDS interop change. Compare against a known-good image tag before changing any product code. | Each of the three is traced to a cause. If it is a real interop fault, it is exactly what Doctor exists to diagnose, and Doctor's own verdict for the pair should be checked against the truth. |
| HAR-8 | Write fixture scratch under `test_output/`, not the source tree. | The vendor fixtures create `test/rti_doctor_fastdds_repr_*/` readiness-marker directories beside the test code. A vendor run therefore dirties the working tree, and they were committed by accident once. Now gitignored, which hides the symptom rather than fixing it. | Point the readiness-control directories at `test_output/`, consistent with `HAR-3`'s failure artifacts. | A vendor run leaves `git status` clean. |
| HAR-7 | Run the live and vendor tiers in CI. | `.github/workflows/rti-doctor.yml` covers lint and the unit tier (**281 tests** as of 2026-08-11), which needs no license. The live tier (**314 tests**, including the H2 concurrency guard, the scale suite and the Q3 representation spike) and the vendor tier need a Connext license, and vendor additionally needs Docker images, so both depend on someone remembering to run them locally. The Q3 spike makes this sharper: it is the only place a DDS *semantic* assumption is verified, and it runs only when someone chooses the live tier. | Provision a self-hosted runner with a licensed Connext install and the vendor images. Add a `live` job on pull requests and a nightly `vendor` job. | Every tier runs without a human deciding to run it. |
| HAR-5 | Add capture assertions for discovery, not only payload. | Existing wire tests validate DATA/DATA_FRAG and encapsulation; the Fast DDS issue was in SEDP timing and TypeInformation. | Add opt-in `tshark` assertions for SPDP builtin endpoint masks, SEDP entity IDs, endpoint GUIDs, topic/type names, and reliability QoS. Keep capture optional outside the dedicated network-evidence tier. | A Fast DDS discovery regression test confirms `0x00000c3f`, `0x000004c2 -> 0x000004c7`, and `PID_RELIABILITY` are present. |

## Priority 2: Improve Doctor Diagnostics

| ID | Improvement | Evidence | Implementation | Acceptance check |
|---|---|---|---|---|
| DOC-1 | Distinguish no counterpart from incomplete discovery. | `qos.no_counterpart` is correct for an empty system but indistinguishable from a late observer missing an endpoint announcement. | Include discovery age, participant presence, known remote builtin endpoint mask, and endpoint-announcement activity in the finding evidence. When a peer advertises the relevant SEDP announcer but no endpoint is observed, add an advisory stating that discovery may be incomplete. | A captured Fast DDS late-observer scenario gives an actionable incomplete-discovery advisory instead of implying no reader exists. |
| DOC-2 | Surface TypeInformation decode failure explicitly. | Connext logs `DISCBuiltin_deserializeTypeInformation: FAILED TO DESERIALIZE`, then Doctor emits generic `type.no_type_info`. | Capture this known discovery/type lookup error through the listener or Doctor debug log and attach it to `type.no_type_info` evidence. Add a distinct, actionable subtype or finding ID if the error can be correlated to the endpoint. | Fast DDS TypeInformation failure states that metadata deserialization failed, identifies the peer, and does not present the condition as merely absent propagation. |
| DOC-3 | Done, not backlog: make headless JSON output machine-safe. | Decision H1 made the text report the only output contract and retired `--format json`. | Delivered: `--format` and `report.render_json` are removed, and `test/doctor_e2e.parse_report` reads the text report instead. | Met: `--format` is rejected by the parser, and the vendor suites assert on finding ids and severities read back out of the text report. |
| DOC-4 | Expose discovered SEDP/QoS details in the report appendix. | TShark proved that the Fast DDS reader advertised RELIABLE, XCDR1, topic, and type even when type metadata was not decoded. | Where discovery bindings expose a field, include raw endpoint key/GUID, vendor ID, QoS policy values, and type-state transition timing in Markdown/text report evidence. Do not infer fields unavailable to Doctor. | A static report for a discovered peer includes enough evidence to explain every `qos.rxo_mismatch` without a PCAP. |

## Priority 3: Expand Coverage Once P0 Is Stable

| ID | Improvement | Implementation | Acceptance check |
|---|---|---|---|
| MAT-1 | Extend the Doctor-asserted Connext/Cyclone matrix from reliability to all existing RxO scenarios. | Reuse `test_rxo_vendor_e2e.py` scenario names, exercising one policy per subtest and both vendor directions. | Each incompatible pair has endpoint no-data evidence and active `qos.rxo_mismatch` naming the changed policy; paired controls have no active `ERROR`. |
| MAT-2 | Extend Fast DDS QoS fixtures beyond reliability. | Add only QoS controls that the current Fast DDS fixture can set and announce; document unsupported policies explicitly. | Each supported policy has a paired healthy/fault Doctor test in both directions. |
| MAT-3 | Add type-layout negative controls. | Measure FINAL/APPENDABLE incompatible layout behavior for each vendor direction before fixing expected report IDs. | No incompatible schema returns `payload FULL`; vendor-specific match and diagnosis behavior is asserted. |
| MAT-4 | Add endpoint-disposal tests. | Stop a discovered fixture endpoint, wait for builtin lifecycle disposal, and sweep again. | Doctor removes the departed endpoint and does not emit a stale probe failure. |
| MAT-5 | Add large-data cross-vendor tests. | Reuse or add fragmentation fixtures and validate the packet capture appendix separately from error severity. | `data.fragmentation` is informational while payload flow remains healthy. |

## From the 2026-08-11 Session

Raised while fixing H8/H9/M13 (`ccaaa7b`) and measuring Q3 (`4aed446`). The
defect-shaped results are findings N1-N9 in
[CODE_REVIEW_2026-08-07.md](CODE_REVIEW_2026-08-07.md#findings-from-the-2026-08-11-session);
what follows is the work those imply, plus the environment gaps that run showed
up. `REP-1` blocks a decision and is the only one of these that is on anyone's
critical path.

| ID | Improvement | Evidence | Implementation | Acceptance check |
|---|---|---|---|---|
| WIRE-1 | **Fixed 2026-08-11.** `read_product_versions` takes the version from parameter `0x8000`'s own bytes through a PDML pass, and `summarize_discovery` merges that with whatever the named subfields yielded, so a Wireshark that does name them agrees with itself instead of reporting a peer twice. The vendor gate stays on the *reporting* side - only `0x010f` contributes to a list the report labels "Fast DDS versions advertised" - even though the read itself is vendor-neutral. Verified against the sim: `TestFastDDSWireE2E` now asserts the reported version against the image tag, which is the check whose absence let this survive `C1`. Original entry follows. Read the Fast DDS product version from the PID the peer actually sends. The flagship Fast DDS evidence feature returns nothing against Fast DDS. | Measured 2026-08-11 against the `rti-doctor-fastdds-e2e:3.6.2` image on Wireshark 4.4.9, capturing its SPDP on the loopback. The peer **does** advertise its version: SPDP carries parameter `0x8000` with `parameterData: 03060200`, which is 3.6.2.0 and matches the image exactly. Wireshark renders `rtps.param.product_version.*` only for RTI's vendor id `0x0101`; the identical PID from eProsima's `0x010f` dissects as `Unknown (0x8000)`. Doctor asks tshark for the named subfields, so a Fast DDS peer contributes no version and `Run capture to ascertain` resolves to nothing after a capture that did see the bytes. Why no test caught it: every product-version test builds synthetic tshark rows, and none has ever asserted a version read from a real Fast DDS peer. `M2` (fixed 2026-08-11) hardened the assembly of those subfields and does not change this - the subfields never arrive. **Feasibility settled 2026-08-11; the approach is measured, not assumed.** Read the parameter through `-T pdml` rather than `-T fields`. PDML nests every RTPS parameter as a node carrying its own `rtps.param.id` and `rtps.parameter_data`, so an id and its bytes arrive already paired and the objection below does not apply. Proven against our own sim, where the image tag is the ground truth: the 3.6.2 container's participant reads back as `3.6.2.0` from the bytes `03060200`. **One reader covers both vendors** - RTI's `0x0101` uses the same PID `0x8000`, and the dissector emits `rtps.parameter_data` alongside its named subfields, so a Connext peer in the same capture reads back as `7.7.0.0` through the identical path with no vendor branch. Filter the pass with `-Y "rtps.param.id == 0x8000"` to keep the PDML small; note it is a second tshark pass over the capture, which compounds `N1`. The original objection, now resolved: the flat `-T fields` view emits raw data only for parameters the dissector leaves undissected - three values against thirteen ids in the measured frame - so those two columns cannot be paired by position, which would have been `M2`'s defect in a new place. Still worth checking whether a Wireshark newer than 4.4.9 names `0x8000` for `0x010f`, which would fix this with a documented tshark floor instead of code. | A capture containing a real Fast DDS peer reports its version, asserted by a vendor-tier test against the pinned image version rather than against a synthetic row. |
| REP-1 | **Fast DDS half done (2026-08-11); Cyclone outstanding.** `test_fastdds_representation_spike.py` answers it for Fast DDS: a writer that never sets the policy advertises an empty sequence, that emptiness behaves as XCDR1 (matched by an XCDR1 reader, refused by an XCDR2-only one with `DataRepresentation` named), and rti_doctor calls the refused pair `compatible` - the Q3 defect, cross-vendor. Two of two measured vendors agree, so the Q3 verdict decision is unblocked for Connext and Fast DDS. What remains is Cyclone, which needs `ENV-1` first, and which the README claims can resolve an unspecified policy to XCDR2 - the one hypothesis that would break the reading. Original entry follows. Measure what a **non-Connext** writer means by an empty DATA_REPRESENTATION advertisement. | `test_data_representation_spike.py` settled this for Connext: an empty advertisement means XCDR1, and a default writer really is refused by an XCDR2-only reader. The README's own Cyclone note says Cyclone resolves an unspecified policy from the type's defaults, which can select **XCDR2** - the opposite meaning from the same wire state. Q3's verdict cannot be decided on Connext evidence alone, and guessing is how the original defect got in. | Extend the spike, or add a vendor-tier sibling, that runs the same matrix with a Fast DDS and a Cyclone writer: what is advertised, whether the pair matches, whether a sample arrives. Then take the Q3 decision and delete the `@unittest.expectedFailure`. | The matrix covers all three vendors; `DESIGN_DECISIONS.md` Q3 moves from "Evidence gathered; decision pending" to Accepted, naming the vendors the reading is claimed for. |
| REP-2 | Establish whether the AUTO sentinel can appear in discovered data at all. | [N6]: no endpoint in the Connext sweep advertised `-1`. A default writer advertises empty and a default reader advertises XCDR1, both while holding AUTO locally. If AUTO is never on the wire from Connext, finding Q5 is an unreachable branch rather than a wrong one, and the effort belongs elsewhere. | One sweep row per way an application can request AUTO, plus the same for the vendors in REP-1. Cheap once REP-1's harness exists. | Q5 is either re-scoped to foreign vendors with evidence, or confirmed reachable and fixed as written. |
| CAP-1 | Give capture artifacts a retention policy. | [N2]: every `c` leaves a PCAPNG and a `.tshark.log` under `test_output/rti_doctor_captures/` and nothing removes them. H8 made this visible; it did not make it bounded. The checkout held 24 leftovers when it was cleared by hand for the 2026-08-11 handover, two of which the vendor tier had written that same afternoon - so the test suites replenish this directory, and a manual clear is a pre-ship step that has to be repeated, not a fix. | Decide the policy before coding it - keep N most recent, delete on clean exit unless the report was saved, or an explicit `RTI_DOCTOR_KEEP_ARTIFACTS` opt-in matching `HAR-3`. Prefer the last, for consistency with the fault artifacts. | A TUI session that runs several captures and exits leaves a bounded, documented set of files. |
| CAP-2 | Let the operator choose a capture interface from inside the TUI. | [N3]: `c` uses `--capture-interface` or `any`, so choosing an interface means quitting and relaunching - and `any` needs the broadest privileges of any choice, so the default is the most privileged one. `wire.capture_interfaces()`, the `tshark -D` enumerator, was deleted with the startup prompt in `ccaaa7b` and is recoverable from that commit. | Restore the enumerator and offer a picker on first `c`, remembering the choice for the session. Keep `--capture-interface` as the non-interactive answer. | An operator can capture on `lo` from a TUI launched with no flags, and the chosen interface appears in the report appendix. |
| CAP-3 | Parse one capture once. | [N1]: `finish()` and `finish_discovery()` each run tshark over the same file, doubling the parse and the peak memory on the known-unbounded `inspect_pcap` path. | Either one tshark invocation whose fields cover both questions, or stream the output instead of buffering it. Measure before choosing: on a bounded capture this may not be worth the complexity. | A capture of N frames runs tshark once, and the wire appendix and Fast DDS version evidence are both still produced. |
| ENV-1 | **Half done 2026-08-12: Cyclone is reinstalled and pinned; the tier still skips rather than fails.** `cyclonedds==11.0.1` is installed in `connext_dds_env_7.7_py311` and pinned in `requirements-dev.txt`, so a rebuilt venv gets it back. The manylinux wheel carries its own Cyclone shared library, so no native build was needed. Eleven skips became nine failures - see `CYC-1`, which is a pre-existing interop condition rather than anything this restore broke. What remains is the second half of the acceptance check: the tier must **fail** rather than skip when a vendor it claims to cover is absent, because the pin alone would not have survived the venv rename that caused this. **Cause established, below.** This is not a host that never had Cyclone - it is a host that had it and lost it. `CYCLONE_CONNEXT_INTEROP_FINDINGS.md` records real runs against the `cyclonedds` Python package **11.0.1** and Cyclone shared library 11.0.1 on 2026-08-03. `run_tests.sh` resolves its interpreter through `python_env.sh`, which names the venv from the detected Connext version *and* Python version - the current one is `connext_dds_env_7.7_py311`, created **2026-08-06 13:10** on Python 3.11.13. So the move to Connext 7.7 on 3.11 built a brand-new venv under a new name rather than upgrading the old one in place, and `cyclonedds` appears in **no** requirements file anywhere in the repo, so nothing reinstalled it. Corroborating: Pylance's local index still holds a `cyclonedds.json` written **2026-08-06 12:29**, forty minutes before that venv existed - an earlier interpreter that did have the package. Neither `cyclonedds` nor `libddsc` is now anywhere on the host, across all four venvs and the system `dist-packages`, so the native library went with it. The suites guard on `importlib.util.find_spec("cyclonedds")`, which is why this degraded to a silent skip instead of an error. Re-measured at `7b57c51`: 33 tests, 12 skipped - **eleven** Cyclone, not twelve (the twelfth is the `VendorWireE2E` base class, and `TestCycloneWireE2E` words its Cyclone skip differently from the other ten, which is why the count never matched the message). Original entry follows. Install Cyclone DDS on the test host. | The 2026-08-11 vendor run skipped **12 of 28 tests** with "Cyclone DDS Python package not available" - every Connext/Cyclone fault control, the whole RxO vendor matrix, and the Cyclone wire E2E. The tier reports OK, so the gap is invisible unless someone reads the skip count. Cyclone is also half of REP-1. | Pin `cyclonedds==11.0.1` - the version the findings doc's evidence was gathered against - in a **test** requirements file. Not `requirements.txt`: `run_rti_doctor.sh` installs that on every end-user launch, which is finding `M16`. Then make the tier fail rather than skip when a vendor it claims to cover is absent - `S1`'s lesson applied to vendors instead of scale. Pinning alone does not survive the next venv rename, which is what actually happened here; the tier has to notice. | `./run_tests.sh vendor` either runs the Cyclone suites or says plainly that it did not. |
| ENV-2 | Make `mypy` available to the tooling. | The review's static baseline reports 11 mypy errors and argues that annotating nine `Optional` fields would make `mypy --strict-optional` a third gate alongside pyflakes. `mypy` is not installed in the venv, so that baseline cannot currently be reproduced or enforced. Related to finding M16: `requirements.txt` is the file the launcher installs on **every end-user launch**, so dev tooling must not go there. | Split a `requirements-dev.txt` (mypy, textual-dev) from the runtime file, as M16 requires anyway, and add mypy to `run_lint.sh` behind a flag once the annotations land. | `run_lint.sh` can run pyflakes and mypy, and an end-user launch installs neither mypy nor textual-dev. |
| EVD-1 | Keep decision evidence where a decision can be reviewed against it. | [N9]: the Q3 matrix is written to `test_output/`, which is gitignored, so it does not survive a clean checkout. The durable record is prose in `DESIGN_DECISIONS.md`, written out longhand for exactly this reason. | Have the spike also write a committed matrix under `docs/evidence/`, or accept prose as the record and stop implying the JSON is durable. Either is fine; the current state implies durability it does not have. | A reviewer with a fresh clone can read the evidence a decision cites. |

## From the 2026-08-12 Session

Cyclone was reinstalled (`ENV-1`), which turned eleven silent skips into nine
real failures. All nine have one cause and none is a product defect, but the
vendor tier is now **red**, and that is what a handover has to account for.

| ID | Improvement | Evidence | Implementation | Acceptance check |
|---|---|---|---|---|
| CYC-1 | The Cyclone data-flow suites assert delivery in the one configuration this project's own evidence says cannot deliver. | Measured at `7b57c51` with `cyclonedds==11.0.1`: `./run_tests.sh vendor` is 34 tests, **9 failures**, 1 skip, 2 expected failures. Every failure is the same shape - whichever endpoint is Cyclone reports `matched: 0`, while the Connext side writes 75-77 samples. Eight are `test_extensibility_vendor_e2e`, all four FINAL/APPENDABLE combinations in both directions, failing on `_assert_data_flows` at `test_extensibility_vendor_e2e.py:68`. The ninth is `TestCycloneWireE2E`, and it is the *same* cause rather than a capture problem: `packets` is filtered to the target writer's GUID prefix and entity id (`wire.py:451-456`), so `packets: 0` for writer `00000202` means that Cyclone writer put no user data on the wire - which is what a writer with no matched reader does. `TestFastDDSWireE2E` passed in the same run through the same capture path, so the capture machinery is fine. This is exactly `CYCLONE_CONNEXT_INTEROP_FINDINGS.md`: under **default** Connext 7.7 TypeObject v2/TypeInformation propagation Cyclone never reciprocally associates, and only `--type-object-v1-only` restores delivery. That doc was written against this same Cyclone 11.0.1, and the fixtures in these suites set no such control. **So these tests cannot have passed since the matrix was added in `439c407` (2026-08-04), and Cyclone disappeared on 2026-08-06, which is what kept it invisible.** Not a regression from this branch: `run_lint.sh`, unit and live are all green at the same commit. | Decide what the suite is for. Either drive the fixtures with the TypeObject-v1-only control, which makes them assert delivery in a configuration that delivers, or keep default propagation and mark them `@unittest.expectedFailure` citing the findings doc - the same treatment the Fast DDS v1-only discovery experiment already gets. Do **not** "fix" this by relaxing `_assert_data_flows`: the assertion is correct and the configuration is what is wrong. Note two of the four combinations are cross-extensibility (final->appendable, appendable->final), which `MAT-3` says should be *measured* before expected results are fixed, so their expectations need settling either way. | The vendor tier is green, or every red test is a recorded expected failure naming the interop condition it reproduces. No Cyclone test asserts delivery under a propagation setting known not to deliver. |
| CYC-2 | Check Doctor's own verdict for the Cyclone pairs now that they run again. | `HAR-6`'s acceptance already asks this question for Fast DDS - "if it is a real interop fault, it is exactly what Doctor exists to diagnose, and Doctor's own verdict for the pair should be checked against the truth" - and `CYC-1` is a confirmed real interop fault sitting untested from that angle. The five Connext/Cyclone fault controls and both RxO vendor directions pass, so Doctor's reliability and RxO findings for this vendor are right; what nothing asserts is what Doctor says about a pair where Cyclone silently never associates. Given `Q3`, the plausible answer is `qos.compatible` and exit 0 on a pair that moves no data. | Point Doctor at one of `CYC-1`'s failing pairs and record the report. If it reports healthy, that is a finding in the `Q3`/`Q4` family - unreadable or absent association rendered as a verdict - and belongs in the review with them. | The report Doctor produces for a known non-delivering Cyclone pair is recorded, and either asserted as correct or filed as a finding. |

## Deferred Diagnostics

| ID | Improvement | Evidence | Implementation | Acceptance check |
|---|---|---|---|---|
| S5 | Harden SPDP2 numeric-bitmask support. | Current tests cover only the compatibility substring fallback; customer use is minimal. | Add direct numeric-mask tests for set and unset bits while retaining the fallback-string compatibility test. | The primary numeric path and fallback path produce the expected result for both enabled and disabled masks. |
| S6 | Re-enable advisory checks only with deterministic coverage. | `blind.security_enabled`, `transport.class_mismatch`, `security.mismatch`, and `discovery.partial` lack direct tests and are disabled from active reporting. | For each advisory, add direct positive and negative tests plus a healthy-path no-findings assertion; then explicitly restore that check to the active set. | Each restored advisory has deterministic coverage that proves it emits only for its intended condition and is absent from a healthy scenario. |
| S7 | Cover system-scan cache freshness. | `Session.system_scan()` supports recent-snapshot reuse for passive screen navigation and forced re-scan for explicit refresh, but tests currently bypass the cache and the view stub ignores `max_age`. | Add deterministic engine tests with a controllable clock and scan-call count for reuse, expiry, and forced re-scan. Rename the view-stub `scope` argument to `captured_at`; keep it non-caching. | Tests prove a scan is reused only inside `max_age`, expired snapshots rescan, and `max_age=0` or explicit `captured_at` always rescans. |
| S8 | Restore payload-health diagnosis. | Payload decode, field walking, decode/drop counters, and payload verdicts are outside the current discovery-and-matching product scope. | Define the report contract, add representative DynamicData fixtures and deterministic positive, negative, and healthy-path tests for each restored check before re-enabling `probe_payload.py` and `typewalk.py`. | Each restored payload-health finding is supported by direct deterministic fixtures, and healthy matching/discovery reports do not depend on payload verdicts. |

## Execution Order

1. Implement FDD-1, because it makes the existing forward Fast DDS P0 test
   repeatable and proves the test architecture.
2. Implement FDD-2 with a minimal fixture and preserved PCAP evidence.
3. Add HAR-1 through HAR-4 while promoting FDD-3; these prevent timing and
   output parsing defects from being misclassified as interoperability results.
4. Implement DOC-1, DOC-2, and DOC-4 so real diagnostic limitations are visible to
   users rather than hidden behind generic findings.
5. Expand the RxO/type/lifecycle matrix only after the P0 controls are reliable.

Inserted 2026-08-11: `REP-1` goes ahead of everything in Priority 2, because it
is the only item here that unblocks a pending decision (Q3) on the flagship
`qos.rxo_mismatch` path, and because `ENV-1` is a prerequisite for half of it.
`CAP-1` and `CAP-2` are small and can be taken whenever the TUI is next open.
`REP-2`, `CAP-3`, `ENV-2` and `EVD-1` are cleanup and can wait.

## Current Baseline

`tools.rti_doctor.test.test_fault_vendor_e2e` passes its four Connext/Cyclone
and four Fast DDS P0 tests when their Docker/runtime prerequisites are present.
The Fast DDS custom FINAL TypeObject compatibility result remains covered as a
specialized expected outcome rather than an older-version support lane.