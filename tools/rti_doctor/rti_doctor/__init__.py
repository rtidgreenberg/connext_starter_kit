"""rti_doctor - DDS interoperability diagnostics for RTI Connext.

Standalone and self-contained: it borrows patterns from tools/rti_spy but imports
nothing from it, so neither tool can break the other.
"""
